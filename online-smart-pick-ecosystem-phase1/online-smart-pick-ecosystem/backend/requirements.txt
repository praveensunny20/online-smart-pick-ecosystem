# FastAPI framework and ASGI server
fastapi==0.115.0
uvicorn[standard]==0.32.0

# Database - PostgreSQL async driver + ORM
sqlalchemy==2.0.35
asyncpg==0.29.0
alembic==1.13.3
psycopg2-binary==2.9.9

# Validation and settings
pydantic==2.9.2
pydantic-settings==2.5.2
email-validator==2.2.0

# Authentication - JWT tokens and password hashing
# bcrypt is pinned to 4.0.1 because passlib 1.7.4 is incompatible with bcrypt >= 4.1
# (passlib inspects bcrypt.__about__.__version__ which was removed in bcrypt 4.1)
python-jose[cryptography]==3.3.0
passlib==1.7.4
bcrypt==4.0.1
python-multipart==0.0.12

# Encryption - for platform credentials (AES-256)
cryptography==43.0.1

# Background jobs - Redis and Celery
redis==5.1.1
celery==5.4.0

# HTTP client for external API calls
httpx==0.27.2

# CORS and middleware
python-dotenv==1.0.1

# Logging
structlog==24.4.0
